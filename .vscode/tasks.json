{
    "tasks": [
        {
            "label": "Build with Make",
            "type": "shell",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/pc_debug" // Укажите путь к вложенной директории
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ],
    "version": "2.0.0"
}